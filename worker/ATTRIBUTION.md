# Attribution

`_worker.bundled.js` is vendored from [tdoc](https://github.com/serenakeyitan/tdoc)
(MIT License, © 2026 Serena Keyitan) with lucius display branding applied.
It provides doc serving from R2, the comment overlay (GitHub sign-in via the
shared OAuth device flow), version routing, and the upload/pull APIs that
lucius publishing rides on. Internal identifiers (`tdoc-*` CSS classes,
`TDOC_*` env vars, `data-tdoc-aid`) are unchanged so the vendored bundle
stays byte-comparable to upstream apart from the branding seds recorded in
the repo history.
